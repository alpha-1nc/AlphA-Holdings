// Transaction 모델이 삭제되어 이 다이얼로그는 더 이상 사용하지 않습니다.
// 입출금 개념은 Report.totalInvestedKrw / totalCurrentKrw로 대체되었습니다.
export function AddTransactionDialog(_: { onSuccess?: () => void }) {
    return null;
}
